/**
 * Hero Swiper slides — MP4 paths under `public/`.
 * Add more entries to rotate multiple videos (same aspect recommended).
 */
export const heroVideos = [
  "/video%20logo/logohero.mp4",
] as const
